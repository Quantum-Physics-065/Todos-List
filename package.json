{
  "name": "mern-todo-vercel",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ../server && npm install",
    "dev": "concurrently \"cd client && npm run start\" \"cd server && npm run dev\"",
    "start": "npm run build",
    "vercel-build": "npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}

